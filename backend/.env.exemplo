# Endereço exato do meu front, lido pelo main.py para montar o CORS.
# É o mesmo valor do .env, e aqui sem valor nenhum, só a chave.
# Troque pelo endereço que o seu front realmente usa.
ORIGEM_FRONTEND=http://localhost:5173
